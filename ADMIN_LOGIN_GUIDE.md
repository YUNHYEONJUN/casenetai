# 🔐 CaseNetAI 관리자 로그인 가이드

**날짜**: 2026-03-01  
**문제**: 기존 관리자 계정으로 로그인 불가  
**원인**: 데이터베이스 비밀번호 변경으로 인한 연결 오류

---

## ⚠️ 현재 상황

`.env` 파일의 `DATABASE_URL`에 설정된 비밀번호가 Supabase에 실제로 적용되지 않아, Node.js 스크립트로 관리자 계정을 생성할 수 없습니다.

**에러 메시지**:
```
❌ 연결 실패: Tenant or user not found
```

---

## ✅ 해결 방법 (2가지)

### 방법 1: Supabase SQL Editor로 직접 계정 생성 (권장) ⭐

1. **Supabase 대시보드 접속**
   - URL: https://supabase.com/dashboard/project/lsrfzqgvtaxjqnhtzebz/sql

2. **SQL 파일 실행**
   - 프로젝트 루트의 `CREATE_ADMIN_SQL.sql` 파일 내용을 복사
   - Supabase SQL Editor에 붙여넣기
   - "RUN" 버튼 클릭

3. **로그인**
   - URL: https://casenetai.kr/login.html
   - 이메일: `admin@casenetai.kr`
   - 비밀번호: `Admin2026!@#$`

---

### 방법 2: Supabase 데이터베이스 비밀번호 재설정 후 스크립트 실행

1. **Supabase에서 DB 비밀번호 재설정**
   - URL: https://supabase.com/dashboard/project/lsrfzqgvtaxjqnhtzebz/settings/database
   - "Database Settings" → "Reset database password"
   - 새 비밀번호를 복사

2. **`.env` 파일 업데이트**
   ```bash
   # .env 파일의 DATABASE_URL을 새 비밀번호로 업데이트
   DATABASE_URL=postgresql://postgres.lsrfzqgvtaxjqnhtzebz:[새_비밀번호]@aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres
   ```

3. **Vercel 환경 변수 업데이트**
   - URL: https://vercel.com/dashboard
   - CaseNetAI 프로젝트 → Settings → Environment Variables
   - `DATABASE_URL` 값을 새 연결 문자열로 변경
   - Redeploy

4. **관리자 계정 생성 스크립트 실행**
   ```bash
   cd /home/user/webapp
   node create-test-admin.js
   ```

---

## 📋 생성되는 계정 정보

### 시스템 관리자
- 📧 **이메일**: admin@casenetai.kr
- 🔑 **비밀번호**: Admin2026!@#$
- 👤 **이름**: 시스템 관리자
- 🎭 **역할**: system_admin
- 💰 **크레딧**: 10,000,000원

### 개발자 계정 (선택사항)
- 📧 **이메일**: dev@casenetai.kr
- 🔑 **비밀번호**: Admin2026!@#$
- 👤 **이름**: 개발자
- 🎭 **역할**: system_admin
- 💰 **크레딧**: 10,000,000원

### 테스트 사용자 (선택사항)
- 📧 **이메일**: test@casenetai.kr
- 🔑 **비밀번호**: Admin2026!@#$
- 👤 **이름**: 테스트 사용자
- 🎭 **역할**: user
- 💰 **크레딧**: 10,000,000원
- 🎁 **무료 체험**: 3회

---

## 🔒 보안 권장사항

1. **첫 로그인 후 비밀번호 변경**
   - 프로필 → 비밀번호 변경
   - 강력한 비밀번호 사용 (최소 12자, 대소문자+숫자+특수문자)

2. **`.env` 파일 보안**
   - ⚠️ 절대 Git에 커밋하지 마세요
   - `.gitignore`에 포함되어 있는지 확인

3. **프로덕션 환경 분리**
   - 개발/스테이징/프로덕션 환경별로 다른 비밀번호 사용
   - Vercel 환경 변수로 관리

---

## 🛠️ 유틸리티 스크립트

### 비밀번호 해시 생성
```bash
node generate-password-hash.js
```
- 새로운 비밀번호의 bcrypt 해시를 생성
- Supabase SQL에서 직접 사용 가능

### 데이터베이스 연결 테스트
```bash
node test-db-connection.js
```
- DB 연결 상태 확인
- 기존 사용자 수 및 테스트 계정 조회

---

## 📞 문제 해결

### 로그인 실패 시
1. **이메일 확인**: `admin@casenetai.kr` (소문자)
2. **비밀번호 확인**: `Admin2026!@#$` (대소문자 구분)
3. **계정 존재 여부**: Supabase SQL Editor에서 확인
   ```sql
   SELECT email, name, role, is_approved 
   FROM users 
   WHERE email = 'admin@casenetai.kr';
   ```

### "이메일 또는 비밀번호가 올바르지 않습니다" 오류
- **원인 1**: 계정이 DB에 없음 → SQL 스크립트 재실행
- **원인 2**: password_hash 불일치 → 해시 값 재생성 후 업데이트
- **원인 3**: is_approved가 false → SQL로 true로 변경
   ```sql
   UPDATE users SET is_approved = true WHERE email = 'admin@casenetai.kr';
   ```

### DB 연결 오류
- **원인**: DATABASE_URL 비밀번호 불일치
- **해결**: 방법 2 참조 (Supabase 비밀번호 재설정)

---

## 📝 관련 파일

| 파일 | 설명 |
|------|------|
| `CREATE_ADMIN_SQL.sql` | Supabase에서 직접 실행할 SQL 스크립트 |
| `create-test-admin.js` | Node.js 관리자 계정 생성 스크립트 |
| `generate-password-hash.js` | bcrypt 해시 생성 유틸리티 |
| `test-db-connection.js` | 데이터베이스 연결 테스트 |
| `.env` | 환경 변수 (비밀번호 포함, Git 제외) |

---

## 🚀 빠른 시작

**가장 빠른 방법 (1분)**:

1. https://supabase.com/dashboard/project/lsrfzqgvtaxjqnhtzebz/sql 접속
2. `CREATE_ADMIN_SQL.sql` 내용 복사 & 붙여넣기 & 실행
3. https://casenetai.kr/login.html 접속
4. `admin@casenetai.kr` / `Admin2026!@#$` 로그인

---

**작성**: AI Developer  
**날짜**: 2026-03-01  
**버전**: 1.0
