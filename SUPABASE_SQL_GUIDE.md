# 📘 Supabase SQL Editor 사용 가이드

## 🎯 Step-by-Step 가이드

### 1단계: SQL Editor 찾기

Supabase 대시보드에서:

1. **왼쪽 사이드바**를 보세요
2. **"SQL Editor"** 메뉴를 찾으세요
   - 아이콘: `</>` 모양
   - 위치: 왼쪽 메뉴 중간쯤
   - "Database" 메뉴 아래에 있을 수 있습니다

또는 이 URL을 직접 여세요:
```
https://supabase.com/dashboard/project/lsrfzqgvtaxjqnhtzebz/sql/new
```

---

### 2단계: 새 쿼리 만들기

SQL Editor 화면에서:

1. 화면 상단에 **"+ New query"** 버튼이 있습니다
2. 또는 이미 큰 텍스트 입력 상자가 보일 것입니다

---

### 3단계: SQL 코드 복사

아래 코드를 **전체 선택**하세요:

```sql
-- 기존 계정 삭제
DELETE FROM credits WHERE user_id IN (SELECT id FROM users WHERE oauth_email = 'admin@casenetai.kr');
DELETE FROM users WHERE oauth_email = 'admin@casenetai.kr';

-- 새 관리자 계정 생성
INSERT INTO users (oauth_email, password_hash, name, role, is_email_verified, is_approved, oauth_provider, oauth_id, created_at, updated_at)
VALUES ('admin@casenetai.kr', '$2b$12$PG6FlhGiMfrki66jR8jDy.Ir2cImvHpHnm8QBJ3p/Na11tSN5CrR2', '시스템 관리자', 'system_admin', true, true, 'local', 'admin_' || extract(epoch from now())::text, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP);

-- 크레딧 생성
INSERT INTO credits (user_id, balance, total_purchased, total_used, free_trial_count, free_trial_used, created_at, updated_at)
SELECT id, 10000000, 0, 0, 0, 0, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP FROM users WHERE oauth_email = 'admin@casenetai.kr';

-- 확인
SELECT u.oauth_email, u.name, u.role, u.is_approved, c.balance FROM users u LEFT JOIN credits c ON u.id = c.user_id WHERE u.oauth_email = 'admin@casenetai.kr';
```

**복사 방법**:
- 마우스로 드래그하여 전체 선택
- `Ctrl+C` (Windows) 또는 `Cmd+C` (Mac)

---

### 4단계: SQL Editor에 붙여넣기

1. SQL Editor의 **큰 텍스트 입력 상자**를 클릭
2. 기존 내용이 있다면 전체 삭제 (`Ctrl+A` → `Delete`)
3. **붙여넣기**: `Ctrl+V` (Windows) 또는 `Cmd+V` (Mac)

---

### 5단계: 실행하기

1. 오른쪽 아래 또는 상단에 **"Run"** 버튼이 있습니다
   - 초록색 버튼
   - 또는 삼각형 ▶ 모양
   - 단축키: `Ctrl+Enter` (Windows) 또는 `Cmd+Enter` (Mac)

2. **"Run" 버튼을 클릭**하세요

---

### 6단계: 결과 확인

실행 후 화면 아래에 **결과**가 표시됩니다:

```
✅ Success. 4 rows affected.
```

마지막 SELECT 쿼리의 결과로 다음과 같은 테이블이 보여야 합니다:

| oauth_email          | name         | role         | is_approved | balance   |
|---------------------|--------------|--------------|-------------|-----------|
| admin@casenetai.kr  | 시스템 관리자 | system_admin | true        | 10000000  |

---

## 🎨 화면 구성 설명

```
┌─────────────────────────────────────────────────────────┐
│ [Supabase 로고]    Project: lsrfzqgvtaxjqnhtzebz        │
├──────────┬──────────────────────────────────────────────┤
│          │  SQL Editor                                  │
│ 📊 Home  │  ┌────────────────────────────────────────┐  │
│ 🗄️ Table │  │  + New query                           │  │
│ 🔐 Auth  │  └────────────────────────────────────────┘  │
│ 📦 Stor  │                                              │
│ </>SQL   │  ┌────────────────────────────────────────┐  │
│ Editor   │  │                                        │  │
│          │  │  여기에 SQL 코드를 붙여넣으세요          │  │
│          │  │                                        │  │
│          │  │                                        │  │
│          │  └────────────────────────────────────────┘  │
│          │                                              │
│          │  [Run] 버튼 또는 Ctrl+Enter                 │
│          │                                              │
│          │  Results:                                    │
│          │  ┌────────────────────────────────────────┐  │
│          │  │ oauth_email | name | role | ...        │  │
│          │  │ admin@case... | 시스템 관리자 | ...     │  │
│          │  └────────────────────────────────────────┘  │
└──────────┴──────────────────────────────────────────────┘
```

---

## 🔧 문제 해결

### ❓ SQL Editor 메뉴가 안 보여요

**해결책 1**: 왼쪽 사이드바 맨 아래 "Database" 섹션을 펼치세요
- "Database" 클릭 → "SQL Editor" 서브 메뉴

**해결책 2**: 직접 URL로 접속
```
https://supabase.com/dashboard/project/lsrfzqgvtaxjqnhtzebz/sql/new
```

**해결책 3**: 상단 검색창에 "SQL Editor" 입력

---

### ❓ Run 버튼이 안 보여요

**위치**:
- 오른쪽 위
- 또는 SQL 입력창 오른쪽 아래
- 초록색 버튼 또는 ▶ 아이콘

**대안**: 단축키 사용
- Windows: `Ctrl+Enter`
- Mac: `Cmd+Enter`

---

### ❌ 에러가 나요

#### "relation 'users' does not exist"
→ 프로젝트가 올바르게 선택되었는지 확인

#### "syntax error"
→ SQL 코드가 완전히 복사되었는지 확인 (처음부터 끝까지)

#### "duplicate key value"
→ 이미 계정이 존재합니다. 괜찮습니다! 다음 단계로 진행:

```sql
-- 기존 계정 업데이트
UPDATE users SET 
  password_hash = '$2b$12$PG6FlhGiMfrki66jR8jDy.Ir2cImvHpHnm8QBJ3p/Na11tSN5CrR2',
  is_approved = true,
  is_email_verified = true
WHERE oauth_email = 'admin@casenetai.kr';

-- 확인
SELECT oauth_email, name, role, is_approved FROM users WHERE oauth_email = 'admin@casenetai.kr';
```

---

## 🎬 간단 요약

1. 왼쪽 메뉴에서 **SQL Editor** 클릭
2. **큰 텍스트 상자**에 SQL 코드 붙여넣기
3. **Run** 버튼 클릭 (또는 `Ctrl+Enter`)
4. 결과에서 **admin@casenetai.kr** 확인
5. https://casenetai.kr/login.html 에서 로그인!

---

## 📞 여전히 어려우시면

다음 정보를 알려주세요:

1. **현재 보이는 화면 스크린샷**
2. **왼쪽 메뉴에 뭐가 있는지** (Home, Table, Auth 등)
3. **에러 메시지** (있다면)

제가 더 구체적으로 안내해드리겠습니다!

---

**작성**: 2026-03-01  
**난이도**: ⭐ 초보자용  
**예상 시간**: 2분
